# =============================================================================
# MONOREPO GITIGNORE
#
# Maintained for the Full-Stack E-Commerce Marketplace.
# This file ensures that generated artifacts, environment-specific configurations,
# and sensitive data (like API keys and database credentials) are not committed
# to the repository.
# =============================================================================


# -----------------------------------------------------------------------------
# OS, IDE & Editor Configuration
#
# Prevents OS-specific metadata and editor-specific settings from causing
# conflicts across different development environments.
# -----------------------------------------------------------------------------
.DS_Store
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?


# -----------------------------------------------------------------------------
# Dependency Directories
#
# Node modules are managed by package managers (npm/yarn) and must be
# installed locally. They should never be versioned.
# -----------------------------------------------------------------------------
/node_modules
/backend/node_modules/
/frontend/node_modules/


# -----------------------------------------------------------------------------
# Logs, Caches & Generated Reports
#
# These files are runtime-generated and specific to a single session or
# environment. They do not belong in the source history.
# -----------------------------------------------------------------------------
# General logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test coverage reports from Jest
/backend/coverage/
lcov.info

# ESLint cache file
.eslintcache


# -----------------------------------------------------------------------------
# Build Artifacts & User Content
# -----------------------------------------------------------------------------

# Frontend production build output from Vite
/frontend/dist/
/frontend/build/

# Vite internal cache directory
/frontend/.vite/

# User-generated content (e.g., product images via Multer)
/backend/uploads/


# -----------------------------------------------------------------------------
# Sensitive Information & Environment Variables
#
# Environment files contain secrets (database URLs, JWT secrets, Stripe keys)
# and must never be committed. The `.env.example` file serves as a template
# for developers and CI pipelines.
# -----------------------------------------------------------------------------
.env
.env.*
!.env.example
.env.*.local


# -----------------------------------------------------------------------------
# Tooling & Framework Specific
# -----------------------------------------------------------------------------

# Prisma client is generated dynamically from the schema
/backend/src/generated/

# Cypress test run artifacts (videos, screenshots, etc.)
/frontend/cypress/videos/
/frontend/cypress/screenshots/
/frontend/cypress/downloads/
/frontend/cypress/reports/